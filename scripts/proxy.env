# Local proxy credentials (do not commit)
export PROXY_HOST="p.webshare.io"
export PROXY_PORT="80"
export PROXY_PASS="lq58okktak8h"

# Split proxies: discovery and authenticated actions MUST NOT share subnet.
export DISCOVERY_PROXY_USER="jxrtqjko-US-5"
export AUTH_PROXY_USER="jxrtqjko-US-4"

# Explicit Reddit proxy URLs
export DISCOVERY_REDDIT_PROXY_URL="http://${DISCOVERY_PROXY_USER}:${PROXY_PASS}@${PROXY_HOST}:${PROXY_PORT}"
export AUTH_REDDIT_PROXY_URL="http://${AUTH_PROXY_USER}:${PROXY_PASS}@${PROXY_HOST}:${PROXY_PORT}"

# Legacy fallback (kept for compatibility; points to discovery only)
export REDDIT_PROXY_URL="${DISCOVERY_REDDIT_PROXY_URL}"
